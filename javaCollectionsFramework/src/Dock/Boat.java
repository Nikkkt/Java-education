package Dock;

class Boat {
    boolean isTerminal;
    int capacity;

    public Boat(boolean isTerminal, int capacity) {
        this.isTerminal = isTerminal;
        this.capacity = capacity;
    }
}